<%= simple_form_for([@product, @picture], html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :asset, as: :file %>
  </div>

  <hr />
  <div>
    <%= f.button :submit %>
  </div>
<% end %>
